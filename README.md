This project is a FAQ chatbot built using Python with a graphical user interface (GUI).

The chatbot reads questions and answers from a CSV file and uses fuzzy matching to find the most relevant response to user input.

It allows users to type questions and receive answers in real time through an interactive chat window.

Language: Python  
Libraries: tkinter, pandas
